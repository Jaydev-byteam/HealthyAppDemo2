import React from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';

export const minusIcon = <Icon name={'minus'} size={16} color={'white'} />;

export const plusIcon = <Icon name={'plus'} size={16} color={'white'} />;
